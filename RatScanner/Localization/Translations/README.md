# Translation files

Each JSON file in this directory holds the UI strings for a specific language.
The keys must match the identifiers consumed by `LocalizationService` and the
file name should align with the language's culture code (e.g. `es.json`).
